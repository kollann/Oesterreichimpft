import { VaccinedateFormErrorMessages } from './vaccinedate-form-error-messages';

describe('VaccinedateFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new VaccinedateFormErrorMessages()).toBeTruthy();
  });
});
