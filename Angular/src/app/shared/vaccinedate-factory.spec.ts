import { VaccinedateFactory } from './vaccinedate-factory';

describe('VaccinedateFactory', () => {
  it('should create an instance', () => {
    expect(new VaccinedateFactory()).toBeTruthy();
  });
});
